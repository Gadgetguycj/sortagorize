from sort_selection import selection
#from sort_mergesort import mergesort
from sort_bubble import bubble
from sort_insertion import *
import data

print("Starting Test\n\n\n")


for x in range(1, 3):

    # bubble(data.random_small)
    # bubble(data.random_medium)
    # bubble(data.random_large)
    # bubble(data.random_slight_small)
    # bubble(data.random_slight_medium)
    # bubble(data.random_slight_large)

    #selection(data.random_small)
    #selection(data.random_medium)
    selection(data.random_large)
    #selection(data.random_slight_small)
    #selection(data.random_slight_medium)
    #selection(data.random_slight_large)

    # insertion(data.random_small)
    # insertion(data.random_medium)
    # insertion(data.random_large)
    # insertion(data.random_slight_small)
    # insertion(data.random_slight_medium)
    # insertion(data.random_slight_large)

    # mergesort(data.random_small)
    # mergesort(data.random_medium)
    # mergesort(data.random_large)
    # mergesort(data.random_slight_small)
    # mergesort(data.random_slight_medium)
    # mergesort(data.random_slight_large)

    



#print("\n\n\nFinished")

